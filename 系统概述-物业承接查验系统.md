# 物业承接查验系统 — 系统概述

> **目标读者：AI 代码审查者**  
> 本文档描述系统的业务目标、架构设计、数据模型、用户操作流程和技术实现，供 AI 交叉检查代码时使用。中英双语。

---

## 1. 项目背景与目标

### 业务背景

本系统为**越南工业园区的物业承接查验**（Property Inspection & Handover）场景设计。开发商完成建设后，物业公司需要逐项检查设备机房、公共区域、室内设施、移交资料等，记录合格/不合格/免检结果及问题描述，最终输出中越双语的打印表单。

### 核心目标

1. **表单管理**：预设 27 套标准化查验模板（A1-D4），覆盖设备机房、公共区域、室内设施、资料移交四大类
2. **现场查验**：员工在手机上打开系统，逐项勾选 pass/fail/skip，填写问题描述，拍照留证
3. **双语打印**：每张表单必须同时展示中文和越南语，在 A4 纸上精确排版，不能溢出、不能缺页脚签名栏
4. **权限分级**：admin（系统管理员）、manager（项目经理）、employee（查验员工）三级角色
5. **设备维保**：关键设备台账 + 定期维保计划 + 维保记录

---

## 2. 技术架构

```
┌─────────────────────────────────────────────────────┐
│                   Frontend (Vue 3 SPA)               │
│  Vite dev server :5173  →  proxy /api → backend     │
│  Vue Router (hash mode), Pinia state, Axios          │
│  Print: CSS @page A4 simulation + browser print      │
├─────────────────────────────────────────────────────┤
│                   Backend (Express.js)               │
│  Port 3002, JWT auth, multer file upload             │
│  SQLite (better-sqlite3, WAL mode)                   │
│  File: backend/data.db    Uploads: backend/uploads/  │
└─────────────────────────────────────────────────────┘
```

- **前端**: Vue 3 Composition API (`<script setup>`), Pinia, Vue Router (hash), Axios, Vite
- **后端**: Node.js, Express, better-sqlite3, JWT (jsonwebtoken), bcryptjs, multer
- **打印**: CSS A4 卡片模拟（210×297mm）+ 浏览器原生打印，备选 pdfmake（CJK 字体加载有问题）
- **测试**: Playwright 自动化质检脚本（`~/.claude/skills/property-print-qa/`）

---

## 3. 目录结构

```
property management/
├── backend/
│   ├── src/
│   │   ├── index.js          # Express 入口，路由挂载
│   │   ├── db.js             # SQLite 初始化 + 迁移
│   │   ├── auth.js           # JWT 认证中间件
│   │   ├── seed.js           # 种子数据（27 套模板 + admin 用户）
│   │   ├── vi-helper.js      # 越南语翻译辅助
│   │   └── routes/
│   │       ├── auth.js       # 登录/改密/me
│   │       ├── users.js      # 用户 CRUD
│   │       ├── projects.js   # 项目/楼栋/房屋/成员/统计
│   │       ├── templates.js  # 系统模板 CRUD
│   │       ├── forms.js      # 用户表单 CRUD + items
│   │       ├── records.js    # 查验记录 + results
│   │       ├── photos.js     # 照片上传/删除
│   │       └── equipment.js  # 设备台账 + 维保记录 + 手册
│   ├── migrations/           # SQL 迁移脚本
│   ├── uploads/              # 上传照片存储目录
│   ├── data.db               # SQLite 数据库文件
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── App.vue           # 主布局（侧边栏 + router-view）
│   │   ├── main.js           # Vue 入口
│   │   ├── style.css         # 全局样式
│   │   ├── api/index.js      # Axios 实例（JWT 拦截器）
│   │   ├── stores/auth.js    # Pinia 认证状态
│   │   ├── router/index.js   # 路由配置 + 权限守卫
│   │   ├── views/
│   │   │   ├── Login.vue
│   │   │   ├── ProjectList.vue
│   │   │   ├── ProjectDetail.vue    # "我的表单"页面
│   │   │   ├── FormEditor.vue       # 核心查验编辑页
│   │   │   ├── RecordList.vue       # 查验记录列表
│   │   │   ├── PrintPreview.vue     # 打印预览（A4 CSS 卡片）
│   │   │   ├── PrintSelect.vue      # 打印选择页
│   │   │   ├── ReferenceForms.vue   # 系统模板浏览
│   │   │   ├── Settings.vue         # 项目设置
│   │   │   ├── Dashboard.vue        # 统计面板
│   │   │   ├── UserManagement.vue   # 用户管理
│   │   │   ├── Equipment.vue        # 设备列表
│   │   │   └── EquipmentDetail.vue  # 设备详情+维保
│   │   └── utils/
│   │       ├── format.js            # 日期格式化
│   │       ├── title.js             # cleanTitle()
│   │       ├── translations.js      # RESULT_MAP（中越结果映射）
│   │       ├── print-constants.js   # 打印分页逻辑（核心）
│   │       └── ui-check.js          # UI 调试工具
│   ├── public/fonts/                # NotoSansSC 字体
│   └── package.json
├── 系统概述-物业承接查验系统.md      # 本文档
└── 用户操作手册.md                   # 用户操作手册
```

---

## 4. 数据库模型

### 4.1 核心表关系

```
users (用户)
  │
  ├── project_members ──── projects (项目)
  │     │                    │
  │     │                    ├── buildings (楼栋)
  │     │                    │     └── houses (房屋/单元)
  │     │                    │
  │     │                    ├── equipment (设备台账)
  │     │                    │     ├── maintenance_records (维保记录)
  │     │                    │     └── equipment_manuals (设备手册)
  │     │                    │
  │     │                    ├── user_forms (用户表单)
  │     │                    │     └── user_form_items (表单条目)
  │     │                    │
  │     │                    └── inspection_records (查验记录)
  │     │                          ├── inspection_results (查验结果)
  │     │                          └── inspection_photos (查验照片)
  │     │
  │     └── inspection_templates (系统模板)
  │           └── template_items (模板条目)
  │
  └── user_template_access (模板访问授权)
```

### 4.2 关键表说明

| 表 | 作用 | 关键字段 |
|---|------|---------|
| `users` | 用户账户 | role: admin / manager / employee |
| `projects` | 项目 | code (P0001), name, type, address, developer |
| `project_members` | 项目-用户关联 | project_id, user_id, role (admin/member) |
| `buildings` / `houses` | 楼栋/单元 | 位置层级结构 |
| `inspection_templates` | 系统预设模板 | form_id (A1-D4), title, category |
| `template_items` | 模板检查项 | item_name, name_vi, check_standard, standard_vi |
| `user_forms` | 用户从模板创建的表单 | project_id, user_id, template_id, status |
| `user_form_items` | 用户表单条目 | 从 template_items 复制，可自由增删改 |
| `inspection_records` | 提交的查验记录 | user_form_id, template_id, building_id, house_id, submitted |
| `inspection_results` | 每条检查结果 | result (pending/pass/fail/skip), problem_description |
| `inspection_photos` | 现场照片 | filename, original_name, 存储在 backend/uploads/ |
| `equipment` | 设备台账 | name, category, status, maintenance_cycle |
| `maintenance_records` | 维保计划/记录 | scheduled_date, completed_date, status, submitted |

### 4.3 数据流：模板 → 表单 → 记录

```
inspection_templates + template_items    (系统预设，admin 管理)
        │
        │  POST /api/forms  (clone)
        ▼
user_forms + user_form_items            (用户表单，可编辑)
        │
        │  POST /api/records  (create from form)
        ▼
inspection_records + inspection_results (查验记录，填写结果)
        │
        │  POST /api/records/:id/submit
        ▼
submitted = 1                           (永久锁定)
```

---

## 5. 用户角色与权限

| 操作 | admin | manager | employee |
|------|-------|---------|----------|
| 管理系统模板（增删改） | ✓ | ✗ | ✗ |
| 管理用户（增删改密码） | ✓ | ✗ | ✗ |
| 创建/删除项目 | ✓ | ✗ | ✗ |
| 编辑项目信息 | ✓ | ✓ | ✗ |
| 管理楼栋/房屋 | ✓ | ✓ | ✗ |
| 添加/移除项目成员 | ✓ | ✓ | ✗ |
| 设备管理 | ✓ | ✓ | ✗ |
| 查看 Dashboard 统计 | ✓ | ✓ | ✗ |
| 创建/编辑/删除自己的表单 | ✓ | ✓ | ✓ |
| 查看他人的表单和记录 | ✓(全部) | ✓(本项目的) | 仅自己的 |
| 填写/提交查验记录 | ✓ | ✓ | ✓ |
| 打印表单 | ✓ | ✓ | ✓ |

**权限实现方式：**
- Express 中间件：`authMiddleware`（JWT 验证）、`adminRequired`、`managerRequired`
- 路由层面：非 admin/manager 的查询自动加 `user_id = req.user.id` 过滤
- `submitted=1` 的记录禁止修改（后端校验 `if (record.submitted) return 403`）

---

## 6. 用户操作流程

### 6.1 管理员初始设置

1. 登录（默认账户：admin / admin123）
2. 创建项目（ProjectList → 新建项目）
3. 进入项目设置（Settings）→ 添加楼栋 → 添加房屋/单元
4. 进入用户管理（/admin/users）→ 创建 manager 和 employee 账户
5. 添加项目成员（Settings → 成员管理）
6. 可选：管理系统模板（ReferenceForms → 编辑/新增）

### 6.2 员工查验流程（核心）

1. **从模板创建表单**
   - 进入 ReferenceForms（参考表单）→ 浏览 27 套模板
   - 点击某个模板 → 进入 FormEditor 预览
   - 可编辑检查项（双击编辑中越双语内容）
   - 点击「保存为我的表单」→ 克隆到 user_forms

2. **开始查验**（两种方式）
   - 方式 A：ProjectDetail（我的表单）→ 点击表单的「编辑」→ FormEditor 加载
   - 方式 B：RecordList → 点击「+」→ 选择表单 → 带 `&inspect=1` 进入 FormEditor → 自动创建 inspection_record

3. **填写检查结果**
   - 选择位置（楼栋 / 房屋 / 位置描述）
   - 逐项点选：合格 (pass) / 不合格 (fail) / 免检 (skip)
   - 不合格项填写问题描述（problem_description）
   - 上传现场照片（每项最多 6 张，每张 ≤10MB）
   - 填写查验意见（inspector_comment）
   - 自动保存（400ms debounce）

4. **提交记录**
   - 点击「提交」→ POST `/api/records/:id/submit`
   - 记录永久锁定（submitted=1），不可再修改

### 6.3 打印流程

1. **选择要打印的内容**
   - 从 ProjectDetail 点「打印」（空白表单）
   - 或从 RecordList 勾选记录后批量打印（已填写的记录）
   - 数据存入 localStorage（`printRecords` 或 `printBlankTemplates`）

2. **打印预览**
   - 跳转 `/print-preview`
   - 系统加载数据，调用 `splitItems()` 动态分页（1-3 页）
   - 渲染 A4 CSS 卡片（210×297mm），包含：
     - 公司标题 + 表单标题
     - 信息表（项目名、日期、位置、查验人）
     - 数据表（序号、检查项目、检查标准、查验结果）
     - 查验意见（紧贴数据表下方）
     - 页脚签名栏（查验人签字 + 日期，flex 推至页底）
   - 中越双语：中文为主，越南语为 block/inline 附属文本

3. **浏览器打印**
   - 点击「浏览器打印」→ `window.print()`
   - `@media print` CSS：`@page { size: A4; margin: 0 }`，`.no-print { display: none }`

---

## 7. 打印系统设计（关键技术）

### 7.1 架构选择

采用 **CSS DOM A4 卡片模拟 + 浏览器原生打印**（非 pdfmake），原因：
- pdfmake 的 CJK 字体方案不稳定（NotoSansSC 13MB，fontkit 在浏览器端挂起）
- DOM 方案可控性更高，可直接用 CSS 精确控制排版

### 7.2 分页算法（print-constants.js）

```
estimateRowHeight(item) → 估算每行高度 (mm)
  ├── 基础: 7mm (中文行 + padding + border)
  ├── 越南语: +3mm (有 VI 文本时)
  ├── 中文标准换行: +4mm (stdCn > 25 字符)
  ├── 越南语标准换行: +3mm (stdVi > 38 字符)
  ├── 中文标准再换行: +4mm (stdCn > 50 字符)
  ├── 越南语标准再换行: +3mm (stdVi > 76 字符)
  ├── 名称换行: +3mm (nameCn > 14 或 nameVi > 24)
  └── 问题描述: +5mm

splitItems(items) → 贪心分包 + 平衡
  ├── Page 1 capacity: 195mm (297mm A4 - 77mm 表头 - 25mm 留白)
  ├── Page 2-3 capacity: 225mm (297mm A4 - 50mm 表头 - 22mm 留白)
  ├── 逐项贪心装入当前页，超出则换页
  └── 平衡: 若末页 ≤2 项且总数 ≥10，则重新均分
```

**关键约束：**
- Page 1 的 capacity 必须 ≤195mm，否则 B9 表单（超长越南语标准文本）会溢出 A4
- 越南语换行阈值（>38 字符）是 B9 防溢出的关键参数
- 分行阈值的设定基于列宽计算：标准列 40%×210mm=84mm，9pt 中文约 25 字符/行，7pt 拉丁约 60 字符/行

### 7.3 打印页脚布局

```
┌──────────────────────────────┐
│         数据表               │
├──────────────────────────────┤
│ 查验意见：___________        │  ← print-comment (flex-shrink: 0)
├──────────────────────────────┤
│         (空白)               │  ← print-page-filler (flex: 1)
├──────────────────────────────┤
│ 查验人签字：___  日期：___   │  ← print-footer (flex-shrink: 0)
└──────────────────────────────┘
```

---

## 8. 中越双语实现

### 数据库层
- template_items: `name_vi`, `standard_vi` 列
- user_form_items: `item_name_vi`, `check_standard_vi` 列
- inspection_results: `custom_item_name_vi`, `custom_standard_vi` 列
- 所有种子数据硬编码了中越双语内容

### 前端层
- UI 显示: `<span class="vi-inline">` (小号灰色斜体，紧跟在中文后)
- 打印显示: `<span class="vi-print">` (block 级，独立一行) 或 `<span class="vi-print-th">` (表头)
- 结果映射: `RESULT_MAP = { pass: { label: '合格', labelVi: 'Đạt' }, fail: { label: '不合格', labelVi: 'Không đạt' }, skip: { label: '免检', labelVi: 'Miễn kiểm' } }`

### 打印 CSS
```css
.vi-print { display: block; font-size: 0.78em; color: #777; font-style: italic; }
.vi-print-inline { font-size: 0.78em; color: #777; font-style: italic; }
.vi-print-th { display: block; font-size: 0.72em; color: #888; font-style: italic; font-weight: normal; }
```

---

## 9. API 认证机制

- 登录: `POST /api/auth/login` → 返回 JWT token（30 天有效）
- 前端: Axios 拦截器自动附加 `Authorization: Bearer <token>` 头
- 后端: `authMiddleware` 解析 JWT，将 `{ id, username, role }` 注入 `req.user`
- 401 响应 → 前端自动清除 token 并跳转登录页

---

## 10. 关键设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 数据库 | SQLite (better-sqlite3) | 单文件部署，零配置，数据量小（<100MB） |
| 前端路由 | Hash 模式 | 避免服务器端 SPA fallback 配置 |
| 打印方案 | CSS DOM A4 模拟 | pdfmake CJK 字体方案在浏览器端不稳定 |
| 分页算法 | 贪心 + 平衡 | 需要平衡防溢出（B9 长文本）和页面利用率 |
| 语言方案 | 数据库存储双语 | 模板和用户数据独立存储 VI 翻译，不依赖运行时翻译 API |
| 照片存储 | 本地文件系统 (backend/uploads/) | 照片完整复制，不依赖原始路径 |

---

## 11. 已知限制

1. **pdf-builder.js 不可用**: 备选 pdfmake 方案因 CJK 字体（13MB NotoSansSC）在浏览器端 fontkit 挂起而无法使用，当前未集成到打印流程
2. **3 页上限**: splitItems 最多支持 3 页。D1（33 项）是目前最大的表单，刚好 3 页
3. **无组件库**: 所有 UI 组件内联在视图中，未抽取公共组件
4. **单项目模式**: 侧边栏设计为单项目（自动选第一个项目），多项目支持有限
5. **无 i18n 框架**: 双语文本硬编码在模板和种子数据中，非标准 i18n 方案
